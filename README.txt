README: Телеграм-бот магазина цифровых товаров и услуг
Этот бот реализует функционал интернет-магазина с поддержкой:

Цифровых товаров (например, ключи, подписки, сертификаты) с онлайн-оплатой (через скриншоты платежей, оплату с баланса).
Физических товаров с оформлением заказов (доставка или самовывоз).
Услуг (запись на сервисы) с оформлением заявки.
Функциональные возможности
Навигация по каталогу:

Выбор категории, подкатегории и товара/услуги.
Отображение описания товара/услуги с фотографией.
Оформление заказа:

Цифровые товары: онлайн-покупка с проверкой баланса, загрузка скриншота платежа и подтверждение администратором.
Физические товары: оформление заказа с выбором способа доставки (доставка/самовывоз) и указанием адреса.
Услуги: оформление заявки (запись на услугу) с вводом описания проблемы и подтверждением заявки.
Многоязычная поддержка:

Локализация (русский и английский языки) с возможностью смены языка через inline-кнопки.
Административные уведомления:

Автоматическая отправка уведомлений администратору о новых заказах и заявках.
Подтверждение или отклонение заказов/заявок администратором через команды.
Гибкое управление состояниями:

Использование Finite State Machine (FSM) для организации пошагового взаимодействия с пользователем.
Установка и настройка
Клонирование репозитория:

bash
git clone <URL_репозитория>
cd <папка_проекта>
Настройка виртуального окружения:

bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
Конфигурация (файл config.py):

Укажите API-токен бота, зашифрованный ADMIN_ID, путь к базе данных (DB_PATH) и путь к файлу секретного ключа (например, /etc/bot_store_new/secret.key).
Настройте реквизиты платежей (например, для LTC, TRX) и другие параметры.
Инициализация базы данных:

Запустите функцию init_db() для создания таблиц.
Заполните таблицы демонстрационными данными (например, вызовите initialize_demo_products()).
Создайте таблицу Categories и заполните её:
Каждая запись должна содержать: id, safe_id, display_name и logic_type (например, 'digital', 'physical', 'appointment').
Добавьте товары/услуги в таблицу Products, ссылаясь на соответствующую категорию через category_id.
Локализация:

В папке translations создайте или отредактируйте JSON-файлы (например, ru.json, en.json) с необходимыми текстами.
Обновите словарь локализации (если требуется) в файлах JSON.
Настройка словаря категорий:

В файле utils/catalog_map.py отредактируйте или добавьте новые записи:
python

CATEGORY_MAP = {
    "keys": "🔑 Ключи",
    "subs": "🛒 Подписки",
    "misc": "🛍️ Разное",
    "certs": "🎫 Сертификаты",
    "services": "💼 Services",
    # Добавьте новые категории по необходимости
}
REVERSE_CATEGORY_MAP = {v: k for k, v in CATEGORY_MAP.items()}
Запуск бота:

Для ручного запуска:
bash

python3 bot.py
Для автозапуска создайте systemd-сервис (например, bot.service) и запустите его:
bash
Копировать
sudo systemctl daemon-reload
sudo systemctl start bot.service
sudo systemctl enable bot.service
Структура проекта
handlers/ – обработчики команд и callback-запросов:

start.py – запуск бота, приветственное сообщение.
menu.py – обработка главного меню, выбор категорий, языка и пр.
catalog.py – навигация по каталогу, выбор товара/услуги.
purchase.py, order.py, appointment.py – логика покупки цифровых товаров, оформления заказов физических товаров и заявок на услуги.
admin.py – административные команды для подтверждения и отклонения заказов/заявок.
database.py – функции работы с базой данных (инициализация, запросы).

config.py – конфигурационные параметры.

utils/ – вспомогательные модули (локализация, словари категорий, хелперы).

translations/ – JSON-файлы с текстами для разных языков.

data/ – папка с изображениями (приветственное фото, иконки, фото товаров и услуг).

Добавление новых категорий, товаров и услуг
Добавление категории:

В таблице Categories вставьте новую запись с уникальным safe_id, display_name и logic_type (например, 'digital', 'physical', 'appointment').
Обновите файл utils/catalog_map.py, добавив соответствующую запись в CATEGORY_MAP (и автоматически обновится REVERSE_CATEGORY_MAP).
Добавление товара/услуги:

В таблице Products создайте новую запись, указав category_id (ссылка на соответствующую категорию), type (подкатегория), name, description, price, photo_path и quantity.
Локализация и меню:

Если необходимо, обновите файлы перевода в папке translations.
Настройка логики:

Логика обработки покупки, оформления заказа или записи на услугу определяется полем logic_type в таблице Categories. При добавлении новой категории убедитесь, что выбрана корректная логика.
Пример работы бота
Пользователь запускает /start → бот приветствует и выводит главное меню.
Выбор категории → подгружаются подкатегории (на основе данных из таблицы Categories и Products).
Выбор товара/услуги → бот выводит описание, цену, фото товара и кнопки для дальнейших действий (покупка, оформление заказа или заявка).
При оформлении заказа/заявки бот ведёт пользователя через последовательные шаги и отправляет уведомления администратору.
Эта инструкция является базовой и может быть дополнена в зависимости от индивидуальных требований проекта. Если возникнут вопросы по настройке или расширению функционала, обратитесь к документации Aiogram и SQLite или свяжитесь с разработчиком.